<div id="page-header">
  <h1>View Trips</h1>
</div>

<%= render 'search' if @query.display_search_form %>

<h2 id="result-count">
  <%= pluralize @trips.total_entries, "record" %> found.
</h2>

<%= render 'trips', :trips => @trips %>
