<div id="page-header">
  <h1>Editing Override</h1>
</div>

<%= render 'form' %>

<hr>
<h2>This override is used in the following allocations:</h2>
<ul>
  <% @override.allocations.each do |a| %>
    <li><%= link_to a.name, [:edit, a] %></li>
  <% end %>
</ul>
<hr>
<%= link_to 'Back', overrides_path, :class => "action-bttn" %>
