Lowdown is a paratransit data reporting system.

Lowdown can import data from RouteMatch, or can accept manual data
import.  It supports data import by trip, run, or monthly summary.

Lowdown generates reports which can be grouped by time, agency,
funding source, etc in configurable ways.  It also supports
age and ethnicity reporting.

Lowdown is a project of Ride Connection, a Portland-area community
transpotation provider.  It is primarily written by hackers at
OpenPlans.